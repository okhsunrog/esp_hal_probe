[env]
DEFMT_LOG="info"

[build]
target    = "xtensa-esp32s3-none-elf"
rustflags = [
  "-C", "link-arg=-Tdefmt.x",
  "-C", "link-arg=-nostartfiles",
]

[target.xtensa-esp32s3-none-elf]
runner = "probe-rs run --chip=esp32s3"

[unstable]
build-std = ["core"]
